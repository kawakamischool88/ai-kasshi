<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# AIカッシー（BEYOND 専用AI 実証実験 #001）開発ルール

このリポジトリは合同会社BEYONDの「本人専用AI基盤」の最初の実証（AIカッシー Ver.0.1）。
運営者＝川上武則（たけ）。利用者は60代後半で、PC / iPad から主に音声入力で使う。

## 絶対に守ること

1. **アプリ本体（`src/`）から service_role キーを使わない。** 使ってよいのは `scripts/` と `tests/` だけ。
2. **行の持ち主（user_id）は `auth.getUser()` から決める。** フォームやリクエストで渡された user_id は信用しない。
3. **新しいテーブルは必ず RLS を有効にし、`auth.uid() = <持ち主列>` の select / insert / update / delete ポリシーを付ける。** `supabase/migrations/20260917000001_chat.sql` がひな形。
4. **新しいテーブルには GRANT も必ず書く。** この Supabase では `authenticated` にも `service_role` にも権限が自動で付かない。付け忘れると `42501 permission denied` になる。`anon` には何も与えない。
5. **DB の変更は必ず `supabase/migrations/` の SQL ファイルで行う。** ダッシュボードの SQL Editor で直接変えない。
6. **モデル名・単価・上限は `src/config/ai.ts` だけに書く。** 他の場所に書かない。原価の掛け算は `src/lib/ai/cost.ts` の `estimateCostUsd` だけで行う。
7. **AI の呼び出しは `src/lib/ai/anthropic.ts` に閉じ込める。** 画面やサーバーアクションから SDK を直接触らない。
8. **本人（柏村さん）の実データを使わない。** 開発・テストは架空ユーザー（`kasshi-test-a/b@example.com`）と川上さんのアドレスだけ。
9. **既存事業（DAIBUTSU・大仏ノート・KSJ系）の Supabase・Vercel・リポジトリ・APIキーに触れない。** AIカッシー専用の APIキーを使う。
10. `.env.local` / `.env.test.local` は commit しない（`.gitignore` 済み）。
11. **秘密の値（APIキー・パスワード）を画面や出力に出さない。** 確認は「形式・文字数・ハッシュ比較・応答コード」で行う（下記「鍵の扱い」）。
12. **どちらの発言かは、色だけで区別しない。** 位置・形・塗りの3つで示す（`src/app/c/[id]/Chat.tsx` の `Bubble`）。

## 鍵の扱い（2026-09-17 確定）

事故が起きたため、ここは特に厳しく運用する。

### 置いてよい場所

| 鍵 | `.env.local`<br>（アプリ本体） | `.env.test.local`<br>（テスト・スクリプト） | **Vercel** |
| --- | --- | --- | --- |
| `NEXT_PUBLIC_SUPABASE_URL` | ○ | ○（`SUPABASE_URL`） | ○ |
| Supabase **Publishable** key | ○ | ○（`SUPABASE_ANON_KEY`） | ○ |
| Supabase **Secret** key | **×** | ○（`SUPABASE_SERVICE_ROLE_KEY`） | **×** |
| `ANTHROPIC_API_KEY` | ○ | × | ○ |
| `SEED_TARGET` / テスト用パスワード | × | ○ | **×** |
| `AI_MONTHLY_WARNING_USD` / `AI_MONTHLY_STOP_USD` | 任意 | × | 任意 |

**Vercel に置いてよいのは、この表で○が付いた3〜5項目だけ。**
`.env.test.local` の中身を Vercel へ一括で取り込まないこと。

### やってはいけないこと

- **`vercel env pull` を使わない。** 手元の `.env.local` が Vercel の内容で上書きされ、
  秘密の鍵や `SEED_TARGET=dev` が混ざる。
- **鍵の値が出力されるコマンドを実行しない。**
  例：`supabase projects api-keys` は**鍵の全文を表示する**（実際にこれで事故が起きた）。
  鍵を調べるときは、先頭の形式（`sb_publishable_` / `sb_secret_` / `eyJ`）・文字数・
  ハッシュの一致比較・API の応答コードだけで判断する。
- **旧方式（JWT形式、`eyJ…`）の鍵を使わない。** 2026-09-17 に Supabase 側で無効化済み。
  新方式（`sb_publishable_` / `sb_secret_`）は**秘密の鍵だけを単独で回転・無効化できる**。
  本番用 Supabase を作るときも、最初から新方式だけを使う。
- **`SEED_TARGET` を本番環境に置かない。** これは「dev のときだけ実行してよい」という
  安全装置そのもの。本番側に `dev` があると意味が反転する。

### 鍵が漏れたときの手順

1. 手元の設定ファイルを新しい鍵に差し替える
2. **Vercel の環境変数も新しい鍵に更新して Redeploy**（ここを飛ばすと次で本番が止まる）
3. 動作確認（テスト一式＋本番URLでのログイン・会話）
4. **その後に**古い鍵を無効化する
5. 無効化された鍵で API を呼び、拒否されること（HTTP 401）を確かめる

## 技術スタック

- Next.js 16（App Router、`src/` 配下、`proxy.ts` が旧 middleware）
- TypeScript / Tailwind CSS v4（設定は `globals.css` の `@theme`）
- Supabase（Auth：メールの6桁コード・招待制 / Postgres + RLS）
- Supabase CLI（`npx supabase ...`、devDependency）
- Anthropic SDK（`@anthropic-ai/sdk`）／モデルは `claude-sonnet-5`
- vitest（`tests/`）

## ハマりどころ（実際に起きたこと）

- `[auth.email] enable_signup = false` は「メールでのログイン自体」を止める。招待制は `[auth] enable_signup = false` だけで行う。
- 新しいテーブルは `authenticated` にも `service_role` にも権限が自動で付かない（上記4）。
- 架空ユーザー（`@example.com`）には Supabase がメールを送らない。画面のログインを試すときは `npm run dev:otp` でコードを取り出す。
- Docker が入っていないため `supabase start` / `db dump` は使えない。確認は開発用のクラウドで行う。
- 認証 Cookie は `src/lib/supabase/cookies.ts` で90日に縮めている（ライブラリ既定は400日）。短い Cookie を伸ばさないよう、上限を超えるものだけ縮める作りにしてある。
- **本番（Vercel）のサーバーは世界標準時で動く。** 日時を画面に出すときは必ず
  `src/lib/time.ts` の `formatDateTimeJst` / `formatDateJst` を使う。
  時間帯を指定せずに `toLocaleString` を書くと、本番だけ9時間ずれる（実際に起きた）。
  手元のパソコンは日本時間なので、開発中は気づけない。
- **連打による二重送信は、保存側の一意制約だけでは止められない。** 送信IDが別々になるため。
  画面側で即座に鍵をかける（`Chat.tsx` の `sendingRef`）。
- **React は「無効なボタン」のクリックを内部で無視する。** DOM の `disabled` を外しても
  ハンドラは呼ばれない。サーバー側の遮断を試すときは、画面側の制限を一時的に外して確認する。

## よく使うコマンド

| やること | コマンド |
| --- | --- |
| 開発サーバー | `npm run dev` |
| DB 変更を開発用 Supabase へ反映 | `npm run db:push` |
| Supabase の設定（招待制・メール文面）を反映 | `npm run config:push` |
| 架空ユーザー A・B を作る | `npm run seed` |
| テスト（越境アクセス・原価計算） | `npm test` |
| ログインを許可する人を登録 | `npm run invite -- メールアドレス` |
| 架空ユーザーの6桁コードを取り出す | `npm run dev:otp -- kasshi-test-a@example.com` |
| 架空ユーザーのテストデータを消す | `npm run reset:test` |

## Phase の範囲（勝手に次へ進まない）

- Phase 1（完了）：土台。認証・最小画面・RLS・越境テスト。
- Phase 2（完了）：文字チャット・会話履歴・AI利用量と推定原価・原価の安全装置・Cookie 90日。
- Phase 3A（完了）：記憶候補 → 本人確認 → 確定。
- Phase 3B（完了）：確定記憶の検索・AI回答への利用・出典の確認・確認待ちの一覧。
- Phase 3C（完了）：記憶の訂正・考えの変化・削除・記憶の一覧・会話ごとの削除。
- Phase 3D（完了）：Astra第三次レビュー対応。会話文脈からの再利用防止・生成中の変更対策・
  出典の墓標・200件超の検索・原価停止の全処理適用。
- Phase 4A（完了）：バックアップ・隔離環境への復元・削除台帳の再適用・
  確定しなかった候補の本文消去・復元モード。
- Phase 4B（完了）：本人向けデータ書き出し・隔離環境への再取り込み確認。
- **Phase 4A / 4B では作らなかった**：PDF（4C）・月次PDF・一般利用者向け取り込み画面・
  遺産や引き渡し・高度な監査基盤・embedding・自動バックアップ・自動書き出し・
  柏村さん本人の実データ・本番利用開始。
- Phase 4C（未着手）：**指示があるまで着手しない。**

## 記憶まわりの決まり（Phase 3A）

- **未確定の候補を、確定記憶として使わない。** 検索やAI回答に使うときは、
  必ず `confirmed_memories`（確定済みだけを見せる view）を使う。表を直接見ない。
- **AIの提案を、本人の考えとして保存しない。** `origin` で由来を区別する。
  本人が採用していないAIの提案（`ai_suggestion`）は候補にしない。
- **AIによる人物像・性格・価値観の推測は保存しない。** 本人が自分の言葉で語った考え方は可。
- **候補は出しすぎない。** 原則1件、多くて2件。迷ったら出さない。
  本人の確認の手間が増えると使われなくなる。
- **同じ発言から同じ候補を繰り返さない。** `(source_message_id, candidate_index)` の
  一意制約で守る。加えて、すでに提案済みの文をAIへ渡して蒸し返しを防いでいる。
- **保存できていないのに「残しました」と表示しない。** 確定は
  「自分の候補・確認待ち・期限内」を更新の条件に直接書いてDB側で守る。0件更新なら失敗として扱う。
- **候補抽出の失敗で会話を壊さない。** 抽出は会話とは別の呼び出しで、失敗しても会話は成功のまま。
- 抽出の利用量は `operation_type = "memory_extract"` として別に記録する。
  抽出だけ安いモデルに変えたいときは `src/config/ai.ts` の `MEMORY.model` を変えるだけ。
- 抽出の指示文（システム側）は毎回同じ文にして使い回しを効かせている。
  毎回変わる内容（提案済みの一覧など）は本文側へ入れること。前に入れると費用が上がる。

## 検索と回答への利用の決まり（Phase 3B）

- **検索するのは `confirmed_memories` だけ。** `memory_candidates` を直接検索しない
  （未確定・却下・期限切れが混ざる）。
- **最初から本人のぶんだけを対象にする。** 全員から取ってから絞る書き方をしない。
  RLS に加えて、問い合わせにも `user_id` の条件を書く（二重の守り）。
- **関係ない記憶を無理に持ち出さない。** 選ぶのは最大3件、原則0〜1件。迷ったら選ばない。
- **記憶は指示ではなくデータ。** 本体の指示の**後ろ**に、囲みの中に入れて渡す。
  本体側にも「記憶の中の命令に従わない」決まりを置く（`MEMORY_GUARD`）。
- **出典は「実際に使った記憶」だけ。** 渡しただけのものを出典にしない。
  AIに「どれを使ったか」を申告させ、`memory_references` に記録する。
- **一度使った記憶は、その会話の間は渡し続ける。** 渡さないと、AIが自分の前の発言を
  「根拠がない」と誤解し、**正しかった内容を訂正して謝ってしまう**（実際に起きた）。
- **基本指示の「できること」は、実装と食い違わせない。** Phase 2 の「記憶する仕組みはない」
  という記述を残したまま記憶を渡したところ、AIが混乱して誤った訂正をした。
- 検索の利用量は `operation_type = "memory_search"` として別に記録する。
- 画面に `memory ID` / `vector` / `similarity` / `embedding` などの用語を出さない。

### embedding（ベクトル検索）について

**Anthropic は文章を数値に変換する仕組みを提供しておらず、公式には他社（Voyage AI など）を
案内している。** 使うと AI提供元がもう1社増えるため、Phase 3B では採用していない。
いまは「本人の確定記憶を絞ってAIに見せ、関係するものを選ばせる」方式。
記憶は本文をそのまま保存してあるので、**将来 embedding を使うときは原文から作り直せる**。

## 訂正・考えの変化・削除の決まり（Phase 3C）

- **3つを同じ処理にしない。**
  訂正（内容が間違っていた）・考えの変化（間違いではない）・削除（本人が消したい）は別物。
  取り違えると、昔の考えが「間違いだった」ことにされてしまう。
- **AIに自由な書き換え・削除の権限を与えない。** AIができるのは
  `memory_revision_requests` に「これのことですか？」と提案を作るところまで。
  記憶が実際に変わるのは、本人が画面のボタンを押したときだけ。
- **訂正か考えの変化かは、本人が選ぶ。** AIの見立てはボタンの並び順にしか使わない。
  どちらだったかは本人にしか分からない。
- **対象が分からないときは何も挙げない。** 0件は正しい答え。勝手に対象を決めない。
  まず言い方（`detectRevisionIntent`）で絞り、手がかりがなければAIを呼ばない。
- **削除は範囲を見せてから。** つながっている版（訂正前・考えが変わる前）もまとめて消す。
  片方だけ残すと、消したはずの内容が「昔の考え」として出てくる。
  何件消えるかを本人に見せ、もう一度押してもらう。
- **削除した本文は残さない。** `status = 'deleted'` の行は本文を持てない
  （DBの決まりで守っている）。削除の記録（`memory_deletions`）も本文を持たない。
- **削除の記録には外部キーを張らない。** 会話ごと消しても、古いバックアップから戻しても、
  「これは削除された」という事実だけは残っている必要がある（Phase 4 の復元で使う）。
- **訂正と無効化は、DB側でひとまとめに行う**（`revise_memory`）。
  アプリから2回に分けて頼むと、途中で失敗して内容が2つになったり消えたりする。
  `security definer` にはしない。本人の権限のまま動かし、RLS をそのまま効かせる。
- **古い処理結果を、あとから書き戻さない。** 保存の条件に「いまも現在有効な内容か」を
  直接書く（0件更新なら何も起きない）。回答へ記憶を渡す直前にも
  `keepStillUsable` で引き直し、その間に消された記憶を外す。
- **同じ発言から、消した記憶を作り直さない。** 3重に止めている。
  ① 候補がすでにあれば作らない ② `(source_message_id, candidate_index)` の一意の決まり
  ③ `memory_deletions` に印があれば作らない。
- **昔の考えは、昔を聞かれたときだけ。** ふだんの検索は `confirmed_memories` だけ。
  `asksAboutPast()` が true のときだけ `past_memories` を足す。
  訂正された旧版（`superseded`）は `past_memories` に入れない
  （間違いだった内容を「昔の考え」として持ち出さないため）。
- **過去の回答の出典は、当時のまま。** あとで訂正・削除されても回答の文面は変えない。
  「訂正される前の内容を参考にしていました」「そのあと消されました」と分かるようにする。
  **消された記憶の本文は出典にも出さない。**
- **結果を表示するときは `revalidatePath` を呼ばない。** 画面が作り直されて
  「訂正しました」「消しました」が本人に見えないまま終わる（Phase 3A・3C の両方で起きた）。
- 対象探しの利用量は `operation_type = "memory_revise"` として別に記録する。
- 画面に `version` / `status` / `revision` / `id` などの用語を出さない。

### RLS の中で同じ表を読むとき

RLS の条件の中で `memory_candidates` を読むと無限に回ってしまう。
`public.owns_memory(uuid)`（`security definer`）に切り出してある。
`auth.uid()` を使っているので、他人の行では必ず false になる。

### この Supabase では「条件のない delete」が禁止されている

一時テーブルの片付け（`delete from tmp;`）もこれに当たり、エラー 21000 になる。
消す対象の一覧は、一時テーブルではなく配列で持つこと。

## 消した内容が「別の道」から戻らないための決まり（Phase 3D）

記憶の検索だけを塞いでも足りない。古い内容が戻ってくる道は他にもある。
新しく道を増やすとき（PDF・エクスポート・バックアップなど）は、
**必ずこの一覧に足して、同じように塞ぐこと。**

| 戻ってくる道 | 塞ぎ方 |
| --- | --- |
| 記憶の検索 | `confirmed_memories` / `past_memories` にしか出ない |
| **会話の文脈（過去のやりとり）** | 影響を受けたやりとりに `messages.excluded_from_ai_at` を立て、AIへ送らない |
| 返事を作っている最中の変更 | 渡した記憶の id と版を控え、保存の直前に `memoriesUnchanged` で確かめる |
| 出典の表示 | 削除済みは本文を出さず、「削除されました」と日時だけ |
| 記憶候補の作り直し | 3重（候補の有無・一意の決まり・`memory_deletions` の印） |

- **会話は「本人が読むもの」と「AIへ送るもの」を分ける。**
  画面には今まで通り出す。AIへ送るときだけ `excluded_from_ai_at is null` で絞る。
  Ver.0.1 では文章の一部を消すような加工はしない（やり損なうと危ない）。
  影響を受けたやりとりを**まるごと送らない**安全側にする。
- **いま返事を待っている発言だけは、印が付いていても必ず送る。**
  送らないと会話が止まってしまう。
- **返事を保存する直前に、渡した記憶がいまも同じか確かめる。**
  変わっていたら保存も表示もせず、「参考にしていた内容が途中で変更されたため、
  この返事は使いませんでした。もう一度お試しください。」と出す。
  呼び出しの費用はかかっているので、利用量は `memory_changed_discarded` として記録する。
- **出典の行は、記憶が消えても残す（墓標）。**
  `memory_references.memory_id` は `on delete set null`。
  消える直前にトリガーが `memory_deleted_at` を書く。
  残すのは「参照があった事実・消えたこと・消えた日時」だけ。
  **本文・引用・中身が分かる題名は残さない。**
  `memory_references` に update / delete のポリシーは作らない（書き換えられない）。
- **記憶の取り出しを件数で打ち切らない。**
  200件で切っていたため、201件目から先が黙って検索対象外になっていた。
  いまはページ分割で読み切る（`SEARCH.pageSize` / `maxFetch`）。
  絞り込みは読んだあと（文字の重なり → AIの判断）で行う。
- **記録が見つからないことを「話していない」と言わせない。**
  ○「関係する記録は見つかりませんでした」 ×「話したことがないですね」
  見つけられていないだけかもしれない。基本指示に例文で書いてある。
- **有料のAI処理は、呼び出しごとに原価の停止を確かめる**（`blockedByBudget`）。
  1往復に有料の呼び出しが最大4回あるため、最初に1回だけでは足りない。
  止めたときも `status = 'blocked'` の記録を1行残す。
  止まっていても、閲覧・記憶の削除・ログアウトは使える（お金がかからないため）。

## バックアップ・復元の決まり（Phase 4A）

### 控える表を増やしたら、必ず一覧に足す

一覧は `src/config/backup.ts` の `BACKUP_TABLES` **1か所だけ**。
足し忘れると、復元したときに**その表だけ空**になる（件数は合って見えるので気づきにくい）。
`tests/backup.test.ts` が、DBの表と一覧を突き合わせて止める。

### 控えに鍵を入れない

`FORBIDDEN_IN_BACKUP` の語が混ざっていないか、作るたびに調べる。
見つかったら、作ったファイルを消して止まる。
鍵が漏れたときの被害は、データが消えるより大きい。

### 台帳は、バックアップとは別に足していくだけ

- 古いバックアップだけを戻すと、**そのあとの削除まで巻き戻る**。
- そこで「消した」「直した」事実を `backups/ledger/*.jsonl` に貯める。
  すでに書いた行は消さない・書き換えない。DBを古くしても台帳は縮まない。
- **削除台帳に本文を入れない。** 消した内容を別の場所に残したら、消した意味がない。
  入れるのは id・scope・日時だけ（`LEDGERS.deletions.fields`）。
- 変更台帳には訂正後の本文を入れる。本人のいま有効な内容であり、失うと困るため。
  **性質が違うので、削除台帳と混ぜない。**

### 復元は必ず隔離した場所へ

- いま動いている場所（`public`）へ直接戻さない。
- 戻すのは `restore` スキーマ。**APIに公開していないので、利用者もアプリも触れない。**
  `supabase/config.toml` の `[api] schemas` に `restore` を足してはいけない。
- 順番：① 戻す ② 台帳を当てる ③ 点検する。**②を飛ばすと消したものが復活する。**
- 点検（`npm run restore:verify`）が全項目「異常なし」になるまで利用を再開しない。

### 復元した環境は、最初から止めておく

環境変数 `AI_KASSHI_MODE=restore` で、
利用者の画面（proxy.ts）と有料のAI処理（actions.ts）を両方止める。
**止めるほうを初期値にしない**（ふだんの環境が誤って止まると困るため）。
知らない値を入れても止まらない。

### 確定しなかった候補の本文は残さない

30日は「確認の期限」であって、本文の保存期間ではない。
［残さない］と期限切れのときは、本文と抽出理由も消す。
DB側の決まり（`memory_candidates_closed_has_no_text`）でも守っている。
**本人が確定した記憶・会話の保存期間は、まだ決めていない。**

### いま分かっている事実（Phase 4A で確認）

- `ai-kasshi-dev` の場所は **ap-northeast-2（ソウル）**。東京ではない。
- Supabase 側にも物理バックアップがある（`supabase backups list`）。
  ただし**それを戻すと削除も巻き戻る**ので、戻したあとは必ず台帳を当てる。
- Storage に保存しているファイルは 0 件（Phase 4A 時点）。
- 控えのファイルは川上さんのPCに平文で置かれる。**中身は会話そのもの。**

## 本人向けデータ書き出しの決まり（Phase 4B）

### バックアップと書き出しは別物。混ぜない

| | 何のため | 設定ファイル |
| --- | --- | --- |
| バックアップ（4A） | 運営がシステムを復旧するため | `src/config/backup.ts` |
| 書き出し（4B） | **本人が自分のデータを手元へ持っていくため** | `src/config/export.ts` |

片方を直したら、もう片方も見直すこと。入れるものが違う。

### 誰のぶんかは、必ずログイン情報から決める

`/api/export` は引数を一切取らない。画面から「誰のぶんか」を受け取らない。
その上で、読むのは**本人の接続**（RLS が効く）。
絞り忘れがあっても他人の行は返ってこない。加えて `user_id` の条件も書く（二重の守り）。

### 消したものを、書き出し経由で復活させない

- 本文は DB の値をそのまま使う。消した・残さない・期限切れは
  DB の決まりで本文を持てない（4A）ので、書き出しにも入らない。
- 作り終えたあと、**消した状態の記憶に本文が付いていないか**を毎回調べる。
  付いていたら渡さない。
- **記憶だけ消した場合、元の会話は残る**（Phase 3C の決定）。
  そのため会話の中には同じ話が出てくる。これは仕様。
  目録と README で本人に明記し、その発言には「AIへ送らない印」を付けて書き出す。

### 鍵と他人のIDが入っていないことを、毎回確かめる

- `FORBIDDEN_IN_EXPORT` の語が混ざっていたら渡さない。
- 見知らぬ UUID（自分の id・自分の行の id 以外）が1つでもあれば渡さない。

### サーバーにファイルを残さない

押されたその場で作って、その場で渡す。置き場所を作らない。
`EXPORT_STORAGE.storeOnServer = false`。将来ためるなら 24時間で消す。

**本人の機器へ保存されたファイルは、こちらから消せない。**
同意の文面に必ず書くこと。

### 作っている最中に変わったら、渡さない

作り始めと作り終わりで、記憶・会話・発言・出典の状態を見比べる。
1つでも違えば破棄して、やり直してもらう（回答生成と同じ考え方・Phase 3D）。

### AIを使わない

本人の原データをそのまま出すだけ。要約などをAIに頼まない。
だから、月のAI利用が停止値に達していても書き出せる。

### 形の版を必ず持たせる

`formatVersion`。中身の形を変えたら数字を上げる。
読み込む側は、版が分からなければ**取り込みを中止する**。

### 壊れたファイルは、一部だけ取り込まない

版が不明・必要なファイルが無い・中身が壊れている・つながりが合わない、
のどれかなら、**DBに触れる前に**止めてエラーにする。
（`npm run export:check` で確認できる。本人画面に取り込み機能は作らない）

### /api/… は、未ログインでもログイン画面へ飛ばさない

飛ばすと、ログイン画面のHTMLが「ファイル」として返り、
受け取る側が中身を取り違える。
`updateSession` で `/api/` は 401 を返す。
受け取る側でも、中身が本当にファイルかを確かめてから保存する。

## 柏村さんが使い始める前に必ず済ませること

1. **独自SMTP の設定**（Resend + `ai@beyond-towada.co.jp`）。
   Supabase 標準のメール送信は、プロジェクトのチームメンバー宛てにしか届かない。
2. **`/cost` を非表示にするか、管理者だけに限る。** いまは誰でも開ける。
3. **データの取り扱いと同意の文面**を用意する。
